[project]
name = "audio-transcription"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydub>=0.25.1",
    "torch>=2.8.0",
    "transformers>=4.55.4",
]
